# このファイルをコピーして terraform.tfvars を作成してください
# cp terraform.tfvars.example terraform.tfvars

aws_profile      = "default"
aws_region       = "ap-northeast-1"
project_name     = "nextjs-sandbox"                  # プロジェクト名
bucket_name      = "your-unique-bucket-name"         # グローバルでユニークな名前に変更してください
environment      = "dev"
github_repository = "your-username/nextjs-sandbox"  # GitHubリポジトリ（owner/repo 形式）

# OIDC Providerが既に存在する場合はfalse
# 同じAWSアカウントで複数プロジェクトがある場合、2つ目以降はfalseに
create_oidc_provider = true
